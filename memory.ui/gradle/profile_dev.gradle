run.doFirst {
    String lockerRoot = "${projectDir}/test-data"

    systemProperties = [
        'pstash.locker_root': lockerRoot,
        'pstash.temp_dir': "$lockerRoot/temp-data"
    ]

    def tempDir = new File("$lockerRoot/temp-data")
    tempDir.mkdirs()
    tempDir.deleteOnExit()
}